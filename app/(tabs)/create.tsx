import { Text, View } from "react-native";

export default function Create() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Create</Text>
    </View>
  );
}
